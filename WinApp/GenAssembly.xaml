<Window x:Class="Windows.GeneratedAssembly"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
    xmlns:RRAuto="clr-namespace:RRAuto.CustomControls"
    Title="Generated Assembly" Height="800" Width="1000" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" Background="#FFF0F0F0" ResizeMode="CanResize">
    <Grid>
        <wfi:WindowsFormsHost x:Name="FormsHost" Margin="12,12,12,47">
            <!--
            for some unexplained reason when the CodeEditor instance is created here CodeEditor.FormatKeyWord event handler is not called;
            so instead I create this instance in the code behind which seems to work fine
            <RRAuto:CodeEditor x:Name="ScriptTextBox" ReadOnly="True" />
            -->
        </wfi:WindowsFormsHost>
        <Button Height="23" HorizontalAlignment="Right" Margin="0,0,12,12" Name="cmdOK" VerticalAlignment="Bottom" Width="75">OK</Button>        
    </Grid>
</Window>
